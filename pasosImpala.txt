docker exec -it namenode bash
hdfs dfs -mkdir -p /user/impala/ #-p para crear directorios si no existen
hdfs dfs -chown impala /user/impala


docker exec -it impala bash
hdfs dfs -put /userdata hdfs://namenode/user/impala/
hdfs dfs -put /schema hdfs://namenode/user/impala/

impala-shell

CREATE EXTERNAL TABLE IF NOT EXISTS usuarios
STORED AS AVRO
LOCATION 'hdfs://namenode/user/impala/userdata'
TBLPROPERTIES ('avro.schema.url'='hdfs://namenode/user/impala/schema/userdata.avsc');

-- Crear la tabla externa summary en formato CSV en HDFS
CREATE EXTERNAL TABLE IF NOT EXISTS summary (
    pais STRING,
    numUsuarios BIGINT
)
ROW FORMAT DELIMITED
FIELDS TERMINATED BY ','
STORED AS TEXTFILE
LOCATION 'hdfs://namenode/user/impala/tables';  -- Ruta en HDFS

-- Insertar datos en summary
INSERT OVERWRITE TABLE summary
SELECT country, COUNT(*) as numUsuarios
FROM usuarios
GROUP BY country
ORDER BY numUsuarios DESC
LIMIT 10;

#VOLCADO A MYSQL CON SQOOP
docker exec -it sqoop bash
#COMPROBAR conectibilidad a mysql
sqoop eval \
  --connect jdbc:mysql://db:3306/proyecto2 \
  --username admin \
  --password admin \
  --query "SELECT * from summary;"

  #EXPORTAR datos de hdfs a mysql
export HADOOP_USER_NAME=hadoop #No es una variable interna de Hadoop, sino una forma de simular otro usuario sin autenticación real, lo cual no es seguro en producción.
sqoop export \
    --connect jdbc:mysql://mydb:3306/proyecto2 \
    --username admin \
    --password admin \
    --table summary \
    --export-dir /user/impala/tables \
    --input-fields-terminated-by ','

#COMPROBAR correcta exportacion
docker exec -it impala bash
hdfs dfs -ls hdfs://namenode/user/impala/tables
hdfs dfs -cat hdfs://namenode/user/impala/tables/32440a474cfb3637-2bfa547200000002_1004680984_data.0.txt